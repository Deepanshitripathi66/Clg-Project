{
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "start:all": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm start"
  }
}
